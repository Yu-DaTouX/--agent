# 新会话开场指令（复制这段给新会话）

> 直接复制下面整个代码块粘贴到新会话。

```
读 %USERPROFILE%\Desktop\pi-desktop\docs\dev\HANDOFF.md，然后继续。

当前状态：**功能完整，可日常使用；pi 已内置，发布只差 electron-builder**。
  · Electron 44 + Vite 7 + React 19 + TS，真连 `pi --mode rpc` 子进程
  · 对话：流式 / markdown + 高亮 / 工具行（含彩色 diff）/ **回合合并**（一轮 = 一块）
  · 回合内顺序：模型说话 → 工作执行栏 → 回复（用户指定）
  · 输入：文本 / `/` 命令补全 / `@` 文件补全 / `!` 直接跑 shell / 图片（粘贴·拖拽·选文件）
  · **写长文模式**：双击 ↑ 或点拖拽柄进入；Enter 换行、Ctrl+Enter 发送
  · 会话：切换 / 新建 / 重命名 / 复制 / 分叉 / 导出 HTML / 删除
  · 控制：模型与思考档选择器 / 压缩 / 自动压缩与重试 / 队列投递模式 / Ctrl+P·Shift+Tab
  · 记忆：agent 只能写「未确认」，只有用户确认过才当事实注入提示词
  · 右栏：常驻状态栏（上下文 / 任务进度条 / 队列 / 扩展 / 环境 / 操作）
  · 设置：记忆 / **模型接入**（API key + 订阅制引导）/ 外观 / 状态 / 关于
  · 首次启动有引导（4 条可验证的检查项）
  · 主题：深浅双套（含代码高亮的深/浅两套配色）
  · 性能：大会话打开 **486ms**（pi 的 switch_session 要 2780ms，改成直读 JSONL）
  · 验证：`npm run check` 全绿（**70 单测 + 15 个真实应用场景**，连跑两轮稳定）

常用命令：
  npm run dev                                    开发（npm install 即可，字体是 npm 依赖）
  npm run vendor:pi                              抽取内置 pi 运行时 → resources/pi-runtime/（20MB，不入库）
  npm run vendor:pi:check                        校验内置运行时（真起一次 pi 做 RPC 握手）
  npm run check                                  提交前跑：typecheck + build + 单测 + 15 场景
  npm run test:live -- motion auth atPath        只跑新增的场景
  npm run test:live -- e2e image queue           会真调模型的三个场景（花少量额度）
  npm run probe-pi                               单独查「pi 能否被找到并启动」
  YAN_SHOT=x.png YAN_PROMPT='…' npx electron .   真实对话截图

关键约定（改代码前先读）：
  · 协议知识只在 src/main/protocol.ts + normalize.ts + agent.ts
    （渲染端只认 MainPush 补丁；normalize.ts 单独拆出来是为了让
     session-reader 能复用它而不产生循环依赖）
  · 不 import pi 的内部模块；不用 shell 启动 pi
    （execPath + ELECTRON_RUN_AS_NODE + 参数数组）
  · 内置 pi 运行时是**生成物**（`npm run vendor:pi` → resources/pi-runtime/），
    **不要手工改里面的文件**，改抽取脚本；pi 升级后重跑一次
    （脚本会自校验依赖闭包，漏包就非零退出）
  · 改设计令牌先改 docs/design/DESIGN.md，再改 tokens.css
  · 测试必须跑在**真实应用**里（YAN_PROBE 注入），不要在裸 BrowserWindow 里测
  · 测试自动隔离：YAN_USER_DATA / YAN_SESSIONS_DIR / YAN_DATA_DIR /
    **YAN_PI_DIR**（后者护的是 auth.json，里面是真实密钥）
    排查时可 YAN_TEST_ISOLATED=0 关掉隔离
  · grid 的 1fr 一律写 minmax(0,1fr)（npm run lint:css 会拦）
  · **位置跳转不要用 behavior:'smooth'** —— 会被重渲染取消（见 HANDOFF §8）
  · 探针不要用固定 sleep 等 UI（负载高时不够），用轮询
  · 探针不要按会话**标题**找 fixture（标题会被模型重新生成），按 path 找

发布前的事：
  A. ✅ **已推 GitHub**：`https://github.com/Yu-DaTouX/--agent`（public）。
     7 个提交的作者已改绑 `174885674+Yu-DaTouX@users.noreply.github.com`，
     绿格子已排上（GitHub API 核验 7/7 归属 @Yu-DaTouX）。
     注意：仓库名以 `--` 开头，命令行里当参数会歧义 —— origin 已配完整 URL，
     直接 `git push` 即可。旧的 `GITHUB_PAT` 环境变量已失效（401），别再用。
  B. ✅ **pi 内置完成**：不自己 esbuild 打包（5 条硬边界，见 HANDOFF §10.1），
     改为搬运 pi 自带的 `dist/bundle` + 6 个最小依赖 = **20MB**
     （vs 依赖捆绑 424MB）。`npm run vendor:pi` 生成，已入 .gitignore。
     15/15 场景 + 图片场景（wasm 路径）全部跑在内置 pi 上通过。
  C. ⏭ **下一步：electron-builder**（把 20MB 运行时做成 extraResources）+ 字体子集化。
  D. 会话树浏览（get_tree 协议有，没做 UI）—— 覆盖度里唯一还值得补的。

我现在要做的下一步是：____（下面三选一，或直接说别的）

A) electron-builder：把内置 pi 做成 extraResources，出第一个安装包
B) 补会话树浏览 / 工具开关 / 扩展快捷键
C) 先用一用，把不顺的地方告诉我（说具体场景，我去改）
```

---

## 本次会话（2026-09-11 第 6 段）已完成的事

用户原话：「找找 ui 框架 现在的太难看了」。

**没有换框架** —— 结论：问题不在框架，在四个方面（色阶差太小、圆角太保守、
空状态一片死寂、「新对话」虚线边框像坏的）。换 Tailwind/shadcn 只会把
2600 行已调好的 CSS 重写一遍，而这些问题换框架一个也解决不了。

| # | 事项 | 结果 |
|---|---|---|
| 1 | **色阶重做** | `bg-0→bg-4` 每级拉开 ~10 个亮度（旧版只差 7，三栏糊成一片黑）；加 `--border-soft`（分隔线）与 `--edge`（卡片内描边高光） |
| 2 | **圆角放大一档** | 4/8/12/14 → 6/10/14/18 + `--r-full` |
| 3 | **空状态重做** | 抽成独立 `EmptyStream.tsx`：符号 + 记忆现状（N 已确认 / M 未确认）+ **3 条可点建议**（点了填进输入框）+ 斜杠命令提示。这是「难看」的最大来源 |
| 4 | **「新对话」按钮** | `dashed 0.8px` → 实心次要按钮 |
| 5 | **分区标题 10px → 11px** | 太淡读不出结构 |
| 6 | **标题栏控件统一成胶囊** | 之前一个是文字一个是裸图标 |
| 7 | 修 **`--session-dir` 退化**（我上一轮引入） | 传了它 pi 就不按 cwd 建项目子目录，新会话平铺到根目录，与用户已有会话分居两处 |
| 8 | 修 **`--name 砚` 退化**（我上一轮引入） | 每个新会话标题都是「砚」，左栏里一模一样 = 没标题 |
| 9 | 修 **历史消息显示「正在思考…」** | 无耗时的历史思考块改用「思考过程」 |
| 10 | 修 **空名字重命名** | pi 不接受空串（`success:false`），名字设了就清不掉 → 主进程拦住并给理由 |

### 验证结果

| 命令 | 结果 |
|---|---|
| `npm run check` | ✅ 全绿（19 单测 + 设计稿无溢出 + 5 个 live 场景） |
| `npm run lint:css` | ✅ 8 个文件无裸 `1fr` |
| 真实数据 | ✅ 会话/记忆/localStorage 均未被污染（已核查无测试写入的会话名） |

截图：`docs/design/preview/redesign-after.png`（对比 `ui-now` 系列）

### 备份

动 UI 前的完整样式与组件已备份到 `.backup/ui-before-redesign/`（未入 git）。

---

## 本次会话（2026-09-11 第 7 段）已完成的事

用户提了一批界面要求，全部落地。

| # | 要求 | 结果 |
|---|---|---|
| 1 | 窗口按钮按 **Win11**（原来是 mac 圆点） | 右上角 `— □ ✕`，46×32 方形，关闭悬停变红，最大化切「还原」图标 |
| 2 | 侧栏按钮放**左上角** | 标题栏最左上角；左栏内重复的那个已删 |
| 3 | 弹出层**统一动画** | 两套关键帧（上弹 / 下沉）+ 遮罩淡入，所有浮层复用 |
| 4 | 输出**更流式更柔和** | 光标从实心块改成**渐隐细竖线**；新到文字淡入 |
| 5 | 生成中显示 **⠋ 正在处理…** | 盲文点字 10 帧 spinner（`PixelSpinner`） |
| 6 | 对话标签旁的**模型标签删掉** | 已删 |
| 7 | 底部信息条**居中收窄 + 上下文靠右** | 780px 居中（比输入框窄 120px），顺序 `本轮用量 │ 上下文 │ 模型` |
| 8 | 导航轨**拉长 + 动态** | 间距 5→9px、宽 11→14px；鼠标靠近拉到 12px/22px |
| 9 | **标题用模型生成**短总结 | 独立 RPC 进程总结首句话（见 `src/main/title.ts`） |
| 10 | 标题旁**写项目**（无项目写「无项目」） | `▸ pi-desktop` / 虚线框「无项目」 |
| 11 | **删掉「上次聊到」** | 已删 |
| 12 | 用户消息**加背景 + 右移** | 右对齐 + `--bg-3` 背景（文字仍左对齐，中文右对齐难读） |
| 13 | 整体**更贴近终端风格** | 圆角收小（2/3/4/6/8）、助手消息加左侧竖线、工具卡用左侧状态色条 |

### 验证

`npm run check` → **19 单测 + 设计稿无溢出 + 6 个真实应用场景全绿**
（新增 `outline` 场景，导航轨 13 条断言）

### ⚠️ 本次踩的坑（已写进 HANDOFF）

1. **复用主会话生成标题会污染对话 + 让 prompt cache 全失效** → 独立进程
2. **`translateX(百分比)` 的基数是元素自身宽度** → 滑块走短一半，改用 `left` + 实测
3. **合成事件测不了 `:hover`** → DOM 断言对 CSS 伪类无效，这是工具边界
4. **`.outline` 有 `pointer-events:none` 就收不到 `:hover`** → 用 `:has()` 反推
5. **左栏用 grid 数行数会被隐式行撑歪** → 改 flex
6. **失败的工具卡自动展开会推走对话** → 只在「正在跑」时展开
7. **我用 `write` 整体覆盖了 CSS，丢了 2174 行**（靠 git 恢复）→ 大文件只用追加/精确 edit

---

## 本次会话（2026-09-11 第 5 段）已完成的事

用户问「软件现在可以用了么」+ 报了两个 UI 问题 + 问了一个扩展弹窗的来源。
顺带发现并修了一个**真功能缺口**。

| # | 事项 | 结果 |
|---|---|---|
| 1 | **设计稿 grid 溢出**（用户报的「12m 被遮」「按钮边框不见」） | 两个症状同一根因：`.rail` 是 grid 但没写 `grid-template-columns`，隐式列按 max-content 定尺 254.8px > 容器 224px，溢出被右邻盖住。全项目 grid 的裸 `1fr` 一律改 `minmax(0,1fr)` |
| 2 | **自动拦这个坑** | 新增 `npm run lint:css`（查裸 `1fr`）+ `npm run measure:design`（量设计稿溢出，有溢出错就非零退出），都接进 `npm run check` |
| 3 | **任务清单显示**（真功能缺口） | 用户的 `left-info-panel` 有 `panel_todos` 工具让 agent 汇报进度，但桌面端完全不显示 —— agent 白调。现在读会话里的 `custom` entry，渲染在左栏顶部（带进度 2/4、删除线、可折叠、空态不占位） |
| 4 | **启动期通知降级** | 那个「信息面板已启用（overlay 44 列）· /panel …」是扩展的自检通知，讲的是 TUI 的 overlay，桌面端不适用。启动期的 info/warning 只进日志抽屉 |
| 5 | **测试全面隔离**（重要） | 测试之前和用户共用状态 —— 改掉过用户的右栏分区顺序、往会话目录塞过 6 个会话、往记忆里留过 5 条编造的「已确认事实」。现在三个 env 隔离：`YAN_USER_DATA` / `YAN_SESSIONS_DIR` / `YAN_DATA_DIR`，fixture 从真实会话只读拷贝 |
| 6 | **pi 的 `--session-dir` 跟着隔离走** | 否则出现「pi 写 A、左栏读 B」 |

### 验证结果

| 命令 | 结果 |
|---|---|
| `npm run check` | ✅ 全绿（19 单测 + 设计稿无溢出 + 5 个 live 场景），**全部在隔离目录里跑** |
| `npm run lint:css` | ✅ 7 个文件无裸 `1fr` |
| `npm run measure:design` | ✅ 无溢出无遮挡（修复前 3 处越界、rail 差 32px） |

### ⚠️ 本次新踩的坑（已写进 HANDOFF）

1. **grid 的裸 `1fr`** —— 轨道最小值是 `auto`(内容宽)，内容一宽就撑破容器，溢出被右邻盖住。**这个坑出现 3 次了**，现在有 lint 拦。
2. **测试与用户共用状态** —— 三处污染都真实发生过（右栏顺序 / 会话目录 / 记忆里的假事实）。
3. **pi 的 `--session-dir` 不跟着 env 走** —— 隔离时要显式喂给 pi。
4. **删 import 时删多了** —— `homedir` 被删但还在用，主进程启动即崩。
5. **顶层执行 + `const` 的 TDZ** —— 函数声明提升、`const` 不提升，fixture 生成器在初始化前被调用。

---

## 本次会话（2026-09-11 第 4 段）已完成的事

用户要求「先把功能完善了，UI 稍后再说」。所以 UI 用最简形式，全部精力放功能。

| # | 事项 | 结果 |
|---|---|---|
| 1 | **Esc 正确语义** | 按 pi 的约定 `clear_queue` → `abort` → **把排队文本放回输入框**（之前直接丢掉） |
| 2 | **图片输入** | 粘贴 / 拖拽 / 选文件；缩略图、去重、移除；随 `prompt` 的 `images` 发送 |
| 3 | **斜杠命令补全** | `get_commands` → 输入 `/` 弹菜单，方向键 + Enter 补全（你的 `/panel`、`/footer`、`/llama` 都能查到） |
| 4 | **`!` 直执行 shell** | 走 RPC 的 `bash`，**不进模型**；流式输出 + 非零退出码标红；结果会进会话上下文 |
| 5 | **会话管理** | 重命名（`set_session_name`，TUI 的 `/name` 是同一字段）/ 复制 / 分叉 / 导出 HTML / 删除 |
| 6 | **从某条消息分叉** | 用户消息悬停 → 「分叉」（`get_fork_messages` + `fork`） |
| 7 | **模型 / 思考档选择器** | 之前是只读文本，现在是真下拉（69 个模型按 provider 分组） |
| 8 | **自动压缩 / 重试开关** | `set_auto_compaction` / `set_auto_retry` |
| 9 | **消息虚拟化** | virtua，阈值 80 条；240 条只渲染 8 条 |
| 10 | **会话列表读 `session_info`** | 用户起的名字优先于首条消息；`named` 标记区分 |
| 11 | **新建会话可见性** | pi 的会话文件懒创建 → 空会话不落盘 → 左栏补合成条目 |
| 12 | **测试体系重做** | 新增 `test:unit`（19 条，不启动 Electron）+ 5 个 live 场景 + 3 个真模型场景 |

### 验证结果

| 命令 | 结果 |
|---|---|
| `npm run typecheck` | ✅ 无错 |
| `npm run build` | ✅ renderer 1.53MB |
| `npm run test:unit` | ✅ **19/19** |
| `npm run test:live -- live features memory sessions` | ✅ 全绿 |
| `npm run test:live -- e2e image queue` | ✅ 全绿（图片场景断言模型回答「红色」） |

### ⚠️ 本次新踩的坑（已写进 HANDOFF §8）

1. **pi 的会话文件是懒创建的** —— `new_session` 后不落盘，左栏扫不到，点「新对话」毫无反应。补合成条目。
2. **`new_session` 在当前会话为空时不新建** —— 返回同一个 id，别误判成 bug。
3. **`bash_execution_update` 的 id 就是请求 id** —— 必须能在发命令前指定 id 才能接流式。给 `command()` 加了 `opts.id`。
4. **直接执行的 bash 走了「成功长输出折叠」** —— 用户为了看结果才跑的，藏起来等于白跑。加 `defaultOpen`。
5. **附件去重只比已有、不比批内** —— 一次传两条相同的会都进来。
6. **删除会话没校验路径、没拦当前会话** —— 加了目录白名单 + `.jsonl` 校验 + 当前会话拦截。
7. **测试污染用户数据** —— 我在真实 sessions 目录造了 6 个会话、在记忆里留了 5 条**编造的"已确认事实"**（这个最严重，会误导后续对话）。已全部清理，并加了 `YAN_SESSIONS_DIR` 让测试用临时目录。
8. **我报的 4 个「bug」里有 2 个是我的测试断言写错了** —— 查代码之前先看证据。

> 诊断心得仍然是那条：**空状态看不见的问题，只有数据满了才复现**。
> 虚拟化、横向滚动条、虚拟化的横溢都是长会话下才暴露。

---

## 需要用户回答的

| # | 问题 | 当前 |
|---|---|---|
| A1 | 下一步做打包分发，还是先补剩下的（会话树 / 快捷键 / 浅色主题 / 设置面板）？ | 阻塞交付给他人的只有打包 |
| A2 | 产品名「砚」定下来吗？ | UI 与扩展都用「砚」，包名 `yan-desktop` |
| A3 | 浅色主题要不要打磨？ | 令牌齐全，未调 |

## 用户数据现状（我这次操作后）

- **会话**：8 个（与你开始前一致 —— 我创建的 6 个测试会话 + 2 个演示会话已删）
- **记忆**：0 条（我编造的 5 条种子数据已清空）
- **设置**：`~/.pi/agent/yan/desktop.json`（cwd / 主题 / 语言 / 右栏顺序）
